# CAU Ativos

Inventário de computadores e monitores do CAU (Next.js, Prisma, PostgreSQL).

## Desenvolvimento

```bash
docker compose up --build
```

App em [http://localhost:3000](http://localhost:3000).

## Documentação de mudanças

Veja `CHANGELOG.md` para o histórico de versões no padrão `X.Y.Z (Build N)`.
